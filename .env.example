# Local development only. On Vercel, set these in
# Project Settings -> Environment Variables instead - Vercel does not read .env files.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3.1-flash-lite
